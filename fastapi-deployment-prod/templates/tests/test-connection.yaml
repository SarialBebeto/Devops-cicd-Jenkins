apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-deployment-prod.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-deployment-prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-deployment-prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
