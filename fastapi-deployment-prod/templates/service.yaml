apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-deployment-prod.fullname" . }}
  labels:
    {{- include "fastapi-deployment-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-deployment-prod.selectorLabels" . | nindent 4 }}
